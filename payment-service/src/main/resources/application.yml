



server:
  port: 5006

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/payment_yt?createDatabaseIfNotExist=true
    username: root
    password: Govind@123

  rabbitmq:
    port: 5672
    host: localhost
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

razorpay:
  api:
    key: "rzp_test_Shc0DpxrMa0ZCr"
    secret: "SigQYilPFZ326PS55dToGIw2"

stripe:
  api:
    key: "your stripe api key"


eureka:
  instance:
    prefer-ip-address: true

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/