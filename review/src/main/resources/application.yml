

server:
  port: 5008


spring:
  application:
    name: review-service




  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/review_yt?createDatabaseIfNotExist=true
    username: root
    password: Govind@123




eureka:
  instance:
    prefer-ip-address: true

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/

