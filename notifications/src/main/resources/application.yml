

server:
  port: 5007


spring:
  application:
    name: notification-service




  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/notification_yt?createDatabaseIfNotExist=true
    username: root
    password: Govind@123

  rabbitmq:
    port: 5672
    host: localhost
    username: guest
    password: guest


eureka:
  instance:
    prefer-ip-address: true

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/
