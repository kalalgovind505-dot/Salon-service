server:
  port: 5005

spring:
  application:
    name: booking-service

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/booking_yt
    username: root
    password: Govind@123

  rabbitmq:
    port: 5672
    host: localhost
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/